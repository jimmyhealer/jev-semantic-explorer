# Contributing

Python 3.10+. One MCP tool: `codebase_investigate`. Do not wrap `jev_evaluate`. Do not add extra tools.

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e ".[mcp]"
```
