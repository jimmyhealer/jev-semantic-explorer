# Changelog

## 0.1.0

- Chunk index over anything a coding agent can read, BM25 seeds, Jev snap judgments → evidence packet
- One MCP tool: `codebase_investigate`
- Human CLI output by default; `--json` for the full packet
- Spoken name **jevex**; `jevex` CLI alias
