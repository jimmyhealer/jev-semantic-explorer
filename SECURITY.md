# Security

- Never commit `.env`, API keys, or TypeSafe / Jev credentials.
- Explorer is read-only. It does not edit files, run tests, or apply patches.
- Packets may include source excerpts from the repo you point it at. Do not aim it at secrets.

If you find a vulnerability, open a private report (GitHub Security Advisory once the repo is public) instead of a public issue.
